pipeline:
  node:
      image: node
      commands:
        - node -v
  
  docker:
    image: plugins/docker
    repo: test
    registry: eve-registry
    insecure: true
    mirror: https://registry.docker-cn.com
