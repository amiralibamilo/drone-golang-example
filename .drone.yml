workspace:
  base: /go
  path: src/github.com/amiralibamilo/drone-golang-example

pipeline:
  node:
      image: node
      commands:
        - node -v
  python:
      image: python
      commands:
        - python --version
  php:
    image: php
    commands:
      - php --version
