pipeline:
  node:
      image: node
      commands:
        - node -v
        - npm -v
  
  docker:
    image: plugins/docker
    repo: localhost:5000/eve
    registry: localhost:5000
    insecure: true
    mirror: https://registry.docker-cn.com
